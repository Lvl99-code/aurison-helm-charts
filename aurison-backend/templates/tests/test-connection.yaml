apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "aurison-backend.fullname" . }}-test-connection"
  labels:
    {{- include "aurison-backend.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "aurison-backend.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
